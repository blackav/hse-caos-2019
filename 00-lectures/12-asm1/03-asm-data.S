        .text                   // последующие данные/команды будут размещаться в read-only секции .text исполняемого файла
        .global helloptr        // делаем идентификатор helloptr видимым в других единицах компиляции
helloptr:                       // helloptr - это "метка", то есть символическая запись константного адреса
                                // по которому будет размещаться следующая ячейка памяти
        .int    hellostr        // резервируем место для 4 байт, в которое записываем адрес, по которому
                                // находится начало массива hellostr

        .global hellostr
hellostr:
        .asciz  "Hello, world!\n" // байт \0 будет автоматически добавлен в конец строки

        .align  8               // при размещении данных разного размера важно соблюдать требования к выравниванию
        .global mydata
mydata:
        .quad   0x123456789A
nearpi:
        .double 3.16
        .align  4
        .global intarr
intarr: 
        .int    4, 5, 6, 7, 8, 9, 10, 11

        .data                   // последующие данные будут размещаться в секции .data
        .align  4
somevar: 
        .int    1

        .bss                    // последующие данные будут размещаться в секции .bss
somearr:
        .skip   128 * 1024 * 1024, 0
